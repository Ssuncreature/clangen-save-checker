function stableClan() {
    try {
    JSON.parse(text);
    // Add the long if statement collection
  } catch (e) {
      // Do something if it fails
      document.getElementById("clanresults").innerText = "Invalid JSON format for clan.json! You probs have an extra comma or bracket somewhere!";
  }
}
function devClan() {
    try {
    JSON.parse(text);
    // Add the long if statement collection
  } catch (e) {
      // Do something if it fails
      document.getElementById("clanresults").innerText = "Invalid JSON format for clan.json! You probs have an extra comma or bracket somewhere!";
  }
}